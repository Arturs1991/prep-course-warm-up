export {};

function getNumber() {
  return Math.random() * 10;
}

const result = 0.6139197698398635; // call the function created above and put the result inside the variable
console.log(result); // Expected output: {random number}
